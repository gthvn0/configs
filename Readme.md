# Used to configure tools

- *tmux.conf* is using **tpm** plugin. You need to clone the pluing
  - `git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm`

# But also...

Some scripts can be useful so I put them in **scripts/**
